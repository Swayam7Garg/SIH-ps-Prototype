# requirements-core.txt
# Minimal set to run the FastAPI server and perform basic geospatial work.
# Install this first for fast hackathon setup.
# Run `pip install -r requirements.txt` for the full ML stack.

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
numpy==1.26.4
pandas==2.2.2
httpx==0.27.0
